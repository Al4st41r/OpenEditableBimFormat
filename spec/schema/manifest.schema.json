{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "oebf://schema/0.1/manifest",
  "title": "OEBF Manifest",
  "x-oebf-note": "manifest.json uses format + format_version for self-identification instead of $schema",
  "type": "object",
  "required": ["format", "format_version", "project_name", "units", "coordinate_system"],
  "additionalProperties": false,
  "properties": {
    "format":             { "type": "string", "const": "oebf" },
    "format_version":     { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "project_name":       { "type": "string", "minLength": 1 },
    "description":        { "type": "string" },
    "created":            { "type": "string", "format": "date" },
    "author":             { "type": "string" },
    "units":              { "type": "string", "enum": ["metres"] },
    "coordinate_system":  { "type": "string", "enum": ["right_hand_z_up"] },
    "material_library": {
      "type": "string",
      "description": "Optional. Reference to a named standard material library distributed with the oebf tools. Reserved for v0.2. Omit in v0.1 projects. Example value: 'oebf-standard-v1'."
    },
    "files": {
      "type": "object",
      "properties": {
        "model":     { "type": "string" },
        "materials": { "type": "string" },
        "schema":    { "type": "string" }
      }
    }
  }
}

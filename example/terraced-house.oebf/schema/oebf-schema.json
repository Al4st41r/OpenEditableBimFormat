{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "oebf://schema/0.1",
  "title": "OEBF Schema Bundle v0.1",
  "description": "Combined OEBF schema bundle. Entity files reference individual sub-schemas by $id (e.g. oebf://schema/0.1/element). Validators should load the individual schema files from spec/schema/ or from this bundle's schema/ directory.",
  "definitions": {
    "schemaVersion": "0.1.0",
    "entityTypes": ["manifest", "path", "profile", "element", "junction", "array", "materials", "group", "opening", "object", "symbol"],
    "uriPattern": "oebf://schema/{version}/{type}",
    "localResolution": "schema/{type}.schema.json"
  }
}
